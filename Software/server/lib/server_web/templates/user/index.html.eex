<h1>Listing Users</h1>

<table>
    <%= for user <- @users do %> <!-- Going thru all users and displaying them -->
        <tr>
            <td><b><%=user.email %></br></td>
            <td><%= link "View", to: Routes.user_path(@conn, :show, user.id) %></td>

        </tr>
    <% end %>
</table>